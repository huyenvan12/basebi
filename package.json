{
  "name": "basebi",
  "version": "1.0.0",
  "private": true,
  "description": "base·bi — self-hosted knowledge management and note-taking app.",
  "type": "module",
  "scripts": {
    "sync-web": "node scripts/copy-web.js",
    "cap:sync": "npm run sync-web && npx cap sync android",
    "cap:run": "npm run sync-web && npx cap run android",
    "cap:open": "npm run sync-web && npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/preferences": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
